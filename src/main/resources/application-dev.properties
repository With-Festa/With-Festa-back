# 데이터 베이스 설정 
spring.application.name=with-Festa-back
spring.datasource.url = jdbc:postgresql://localhost:5432/festaadmin
spring.datasource.username = festaadmin
spring.datasource.password = cware1234

# Hibernate 설정 
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Swagger 설정
#server.servlet.context-path=/api-docs